# TorrentLeech passkey (required for uploads)
# Get this from your TorrentLeech profile (Torrent Passkey)
TL_ANNOUNCE_KEY=your_announce_key_here

# Flask secret key (REQUIRED - app will not start without this)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Path to your media library on the host
MEDIA_PATH=/path/to/your/media

# Optional: Set to 0 to disable background worker
# TORRUP_RUN_WORKER=1

# Optional: Basic auth (only if exposing outside localhost)
# Leave unset for no auth (fine for local use)
# TORRUP_AUTH_USER=admin
# TORRUP_AUTH_PASS=your_password_here
